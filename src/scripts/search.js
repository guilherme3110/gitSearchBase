import { getCharacterByName } from "./requests.js"


export function searchUser(){
    const search = document.querySelector('.')
    const btn = document.querySelector('.')

    btn.addEventListener('click', (e) => {
        
    })
}